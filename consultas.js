function json(data,status=200){return new Response(JSON.stringify(data),{status,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store"}})}
export async function onRequestGet(context){try{const r=await context.env.CLIENTES_DB.prepare(`SELECT q.id,q.email,q.whatsapp,q.servicio,q.archivo,q.detalle,q.origen,q.campana,q.estado,q.creado_en,c.nombre,c.empresa FROM consultas q LEFT JOIN clientes c ON c.email=q.email ORDER BY q.id DESC LIMIT 300`).all();return json({ok:true,consultas:r.results||[]})}catch(e){console.error(e);return json({error:"No se pudieron cargar las consultas."},500)}}
